<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.navg.laboratory.mapper.common.LoginMapper">

    <select id="login" resultType="cn.navg.laboratory.pojo.User">
        select user.*,role.RoleType from user left join userrole
        on userrole.UserId =`user`.UserId left join role on
        userrole.RoleId=role.RoleId
        where user.UserAccount =#{UserAccount} and `user`.UserPassword= #{UserPassword} and
        role.RoleType =#{roleType};
    </select>
</mapper>